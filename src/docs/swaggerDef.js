const { version } = require('../../package.json');
const config = require('../config/config');

const swaggerDef = {
  openapi: '3.0.0',
  info: {
    title: 'Stackoverflow clone API documentation',
    version,
    license: {
      name: 'ISC',
      url: 'https://github.com/Muilat/stackoverflow-clone',
    },
  },
  servers: [
    {
      url: `http://localhost:${config.port}/v1`,
    },
  ],
};

module.exports = swaggerDef;
